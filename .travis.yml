---
  language: go
  sudo: false
  notifications:
    email: false
  go:
    - 1.5
    - tip
    
  before_install:
    - pip install --user codecov

  install: make deps
  script: make validate && make test

  after_success:
    - codecov